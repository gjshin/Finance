# GPCM 계산기 — 시작하기

한국 상장사 유사기업 배수(GPCM)와 WACC를 계산해 엑셀로 뽑아주는 프로그램입니다.

**`run_kr.bat` 하나만 더블클릭하시면 됩니다.**
나머지 파일은 무시하세요.

---

## 1단계 — 파이썬 설치 (처음 한 번만)

이미 설치돼 있으면 건너뛰세요.

1. <https://www.python.org/downloads/> 접속
2. 노란색 **Download Python** 버튼 클릭
3. 설치 화면 **맨 아래 `Add python.exe to PATH` 체크** ← 이걸 빠뜨리면 안 됩니다
4. Install now

> 체크를 놓치셨다면 파이썬을 지웠다가 다시 설치하시는 게 빠릅니다.

---

## 2단계 — DART API 키 발급 (처음 한 번만, 무료)

DART에서 재무제표를 받아오는 데 필요합니다. **각자 본인 키를 발급받으세요.**

1. <https://opendart.fss.or.kr> 접속
2. 오픈API 이용 신청 → 이메일 인증
3. 발급된 키(영문·숫자 40자)를 복사해 메모장에 저장

> 하루 조회 한도가 키마다 따로 주어집니다. 한 키를 여럿이 나눠 쓰면 금방 한도에 걸립니다.

---

## 3단계 — 실행

**`run_kr.bat` 더블클릭**

- 검은 창이 뜨고 준비가 시작됩니다
- **처음에는 2~5분 걸립니다.** 창을 닫지 마세요
- `WARNING` 으로 시작하는 문구가 보여도 정상입니다
- 준비가 끝나면 브라우저가 자동으로 열립니다

브라우저 화면에서:

1. 왼쪽 **OpenDart API Key** 칸에 발급받은 키 붙여넣기
2. **대상회사의 종목코드를 한줄씩 입력하세요** 칸에 종목코드 입력 (예: `005930`)
3. **Go,Go,Go 🚀** 클릭

끝나면 엑셀 파일을 내려받을 수 있습니다.

**끄실 때는 검은 창을 그냥 닫으세요.**
다음부터는 `run_kr.bat` 더블클릭하면 바로 뜹니다 (준비 과정 없음).

---

## 잘 안 될 때

| 증상 | 해결 |
|---|---|
| `파이썬이 설치되어 있지 않습니다` | 1단계의 **PATH 체크**를 안 하신 겁니다. 다시 설치 |
| `실행 환경을 만들지 못했습니다` | 폴더가 OneDrive·바탕화면 동기화 폴더 안에 있으면 `C:\GPCM` 같은 단순한 경로로 옮기고 재시도 |
| `설치에 실패했습니다` | 회사망 방화벽이 `pypi.org` 를 막는 경우입니다. 전산팀에 문의하거나 다른 네트워크에서 한 번 실행 |
| **DART 서버에 접속할 수 없습니다** | 회사망이 `opendart.fss.or.kr` 을 막는 경우입니다. 전산팀에 허용 요청 |
| 검은 창이 바로 닫힘 | 창에 뜬 메시지를 캡처해서 문의해주세요 |

> **중요:** 이 프로그램은 **국내에서 실행해야** 합니다. DART가 해외 접속을 막습니다.
> 회사 PC나 개인 PC에서 쓰시면 문제없습니다.

---

## 업데이트

코드가 바뀌면 새 ZIP을 받아 **폴더 전체를 교체**하세요.
`.venv` 폴더는 그대로 두시면 준비 과정을 다시 거치지 않아 빠릅니다.

---

## 결과 숫자에 대해

- 배수와 WACC는 **DART 공시 재무제표를 그대로 읽어** 계산합니다
- 엑셀에는 계산에 쓴 값들이 함께 들어갑니다. **조서에 넣기 전 확인하세요**
- 값이 비어 있으면 `0` 이 아니라 **"해당 계정을 찾지 못했다"** 는 뜻입니다
- 아직 공시되지 않은 분기를 고르면 조회 가능한 최근 기간을 알려줍니다

---

## 이 폴더의 나머지 파일들

궁금하실까 봐 적어둡니다. **쓰실 일 없습니다.**

| 파일 | 용도 |
|---|---|
| `GPCM.py` | 해외 상장사용 (별도 앱) |
| `requirements.txt` | 필요한 프로그램 목록 — `run_kr.bat` 이 알아서 씁니다 |
| `test_gpcm_kr.py` | 개발자용 검증 코드 |
