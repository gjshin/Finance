# DART 접속 진단

Streamlit Cloud(해외 서버)에서 DART 조회가 안 되는 것이 **정말 차단 때문인지** 확인합니다.

확인하고 나서 해결책을 고르는 것이 순서입니다. 대안들은 모두 손이 더 가거나 비용이 들기 때문입니다.

---

## 왜 확인부터 하나

원래 앱은 DART에 연결이 안 되면 **2분 넘게 멈췄다가 아무 메시지 없이 실패**했습니다.
"동작하지 않는다"고 알고 계신 것이 **그 증상**일 수도 있습니다. 지금은 10초 안에 원인이 화면에 뜹니다.

---

## 어떻게 하나

**같은 진단을 두 곳에서 돌려 비교합니다.**

### ① 회계사님 PC (기준선)

`run_diagnose.bat` 더블클릭 → 브라우저에서 **진단 시작** → 결과 복사

한국에서 실행하는 것이므로 **정상**이 나와야 정상입니다.

### ② Streamlit Cloud (비교 대상)

Streamlit Cloud에서 앱을 하나 만들고 **Main file path**를 `diagnose_dart.py`로 지정 →
**진단 시작** → 결과 복사

### ③ 두 결과를 붙여주세요

---

## 결과 읽는 법

| 판정 | 의미 | 다음 |
|---|---|---|
| **정상** | DART가 응답함 | 차단이 아닙니다. 그냥 쓰시면 됩니다 |
| **연결실패** | 응답 자체가 없음 | 차단 또는 네트워크 문제 |
| **차단의심** | DART가 아니라 HTML 페이지가 옴 | 차단 확정 |
| **판단불가** | 형태를 알 수 없는 응답 | 원문을 보고 판단합니다 |

**API 키는 없어도 됩니다.** DART는 잘못된 키에도 정상 응답을 돌려주기 때문에, 응답이 오는지만 봐도 도달 여부를 알 수 있습니다.

---

## 차단이 확인되면

DART 호출이 **한국 IP에서 나가야** 합니다. 세 가지 방법이 있습니다.

| 방법 | 비용 | 팀 공유 | PC 켜둬야 하나 |
|---|---|---|---|
| **로컬 실행** (`run_kr.bat`) — 이미 됩니다 | 0원 | ❌ | — |
| **로컬 + 터널** | 0원 | ✅ | 예 |
| **국내 클라우드 서버** | 월 1~3만원 | ✅ | 아니오 |

어느 쪽이 맞는지는 **누가 쓰는지**에 달렸습니다. 결과 보내주시면 같이 정하겠습니다.

---

## 개발자용

```bash
python test_diagnose_dart.py   # 판정 로직 테스트 (네트워크 불필요)
streamlit run diagnose_dart.py
```

판정 로직은 `classify()` 하나에 모여 있고 화면과 분리되어 있습니다.
