# MCP 서버 전용 의존성.
#
# 앱(requirements.txt)과 별도의 가상환경(.venv-mcp)에 설치합니다.
# mcp 패키지가 요구하는 starlette 버전이 streamlit과 충돌하기 때문에
# 같은 환경에 섞어 설치하면 앱이 깨질 수 있습니다.
mcp==2.0.0
OpenDartReader==0.2.2
finance-datareader==0.9.202
pandas==3.0.5
requests==2.33.1
lxml==6.1.1
beautifulsoup4==4.15.0
